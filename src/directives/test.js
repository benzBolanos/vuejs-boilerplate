// import Vue from 'vue'

// Vue.directive('demo', {
//   bind: (el, binding, vnode) => {
//     el.innerHTML = `testing!`
//   }
// })

const test = (el, binding, vnode) => {
  el.innerHTML = `testing!`
}

export {
  test
}
